import { Search, UploadCloud } from "lucide-react";
import { useState } from "react";
import { DocumentList } from "../components/library/DocumentList";
import { UploadDropzone } from "../components/library/UploadDropzone";
import { Badge } from "../components/ui/Badge";
import { useDocuments } from "../hooks/useDocuments";

export function LibraryPage() { const [search, setSearch] = useState(""); const [showUpload, setShowUpload] = useState(false); const { documents, stats, loading, error, upload, remove } = useDocuments({ search: search || undefined }); return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-slate-500">Không gian tài liệu</p><h1 className="mt-1 text-2xl font-semibold tracking-tight text-white">Thư viện của bạn</h1><div className="mt-3 flex flex-wrap gap-2"><Badge tone="success">{stats.ready} sẵn sàng</Badge><Badge tone="warning">{stats.processing} đang xử lý</Badge><Badge>{stats.total} tài liệu</Badge></div></div><button onClick={() => setShowUpload(!showUpload)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary-500 px-4 py-2.5 text-sm font-medium text-white shadow-glow hover:bg-primary-400"><UploadCloud className="h-4 w-4" />Upload PDF</button></div>{showUpload && <UploadDropzone onUpload={async (...args) => { await upload(...args); setShowUpload(false); }} />}{error && <p className="rounded-xl bg-rose-400/10 px-4 py-3 text-sm text-rose-300">{error}</p>}<div className="relative"><Search className="pointer-events-none absolute left-3.5 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-600" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Tìm theo tên tài liệu…" className="w-full rounded-xl border border-white/10 bg-white/[0.04] py-3 pl-10 pr-4 text-sm text-white outline-none placeholder:text-slate-600 focus:border-primary-400/60" /></div>{loading ? <div className="py-16 text-center text-sm text-slate-600">Đang tải thư viện…</div> : <DocumentList documents={documents} onDelete={remove} />}</div>; }

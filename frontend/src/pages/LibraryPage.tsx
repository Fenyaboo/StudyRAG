import { Search, UploadCloud } from "lucide-react";
import { useState } from "react";
import { DocumentList } from "../components/library/DocumentList";
import { UploadDropzone } from "../components/library/UploadDropzone";
import { Badge } from "../components/ui/Badge";
import { useDocuments } from "../hooks/useDocuments";

export function LibraryPage() { const [search, setSearch] = useState(""); const [showUpload, setShowUpload] = useState(false); const { documents, stats, loading, error, upload, remove } = useDocuments({ search: search || undefined }); return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[11px] font-semibold uppercase tracking-[0.18em] text-carbon/45">Không gian tài liệu</p><h1 className="mt-2 font-display text-2xl font-extrabold tracking-[-0.03em] text-carbon">Thư viện của bạn</h1><div className="mt-3 flex flex-wrap gap-2"><Badge tone="success">{stats.ready} sẵn sàng</Badge><Badge tone="warning">{stats.processing} đang xử lý</Badge><Badge>{stats.total} tài liệu</Badge></div></div><button onClick={() => setShowUpload(!showUpload)} className="inline-flex items-center justify-center gap-2 rounded-full bg-carbon px-5 py-3 text-sm font-semibold text-cream transition hover:shadow-hard-accent"><UploadCloud className="h-4 w-4" />Upload PDF</button></div>{showUpload && <UploadDropzone onUpload={async (...args) => { await upload(...args); setShowUpload(false); }} />}{error && <p className="rounded-xl border border-accent-500/25 bg-accent-100/70 px-4 py-3 text-sm text-accent-600">{error}</p>}<div className="relative"><Search className="pointer-events-none absolute left-3.5 top-1/2 h-4 w-4 -translate-y-1/2 text-carbon/35" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Tìm theo tên tài liệu…" className="w-full rounded-xl border border-carbon/12 bg-white py-3 pl-10 pr-4 text-sm text-carbon outline-none transition placeholder:text-carbon/35 focus:border-accent-500 focus:ring-4 focus:ring-accent-500/12" /></div>{loading ? <div className="py-16 text-center text-sm text-carbon/50">Đang tải thư viện…</div> : <DocumentList documents={documents} onDelete={remove} />}</div>; }
